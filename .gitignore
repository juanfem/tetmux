# SwiftPM
/.build/
/.swiftpm/
Package.resolved.bak
*.xcodeproj

# Xcode
DerivedData/
*.xcuserstate
xcuserdata/

# macOS
.DS_Store

# Packaging output
/dist/

# The R3.6 tmux matrix: sources and binaries built by Scripts/build-tmux-matrix.sh. A means, not an
# artifact — what gets committed is the fixtures they were used to capture.
/.tmux-matrix/
